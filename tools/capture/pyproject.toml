[project]
name = "spacepit-sample-bank-capture"
version = "0.1.0"
description = "Chromatic capture tool for thespacepit master sample bank"
requires-python = ">=3.10"
dependencies = [
    "mido>=1.3.0",
    "python-rtmidi>=1.5.0",
    "sounddevice>=0.4.6",
    "soundfile>=0.12.1",
    "numpy>=1.26.0",
]
